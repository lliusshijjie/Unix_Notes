syntax = "proto3";

package minirpc.proto;

option cc_generic_services = true;

service CalculatorService {
    rpc Add(AddRequest) returns (AddResponse);
}

message AddRequest {
    int32 lhs = 1;
    int32 rhs = 2;
}

message AddResponse {
    int32 result = 1;
}
